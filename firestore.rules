rules_version = '2';

service cloud.firestore {
  match /databases/{database}/documents {
    // All access goes through the CMS API (Admin SDK), which bypasses these
    // rules entirely. No client-side SDK reads/writes are expected, so deny
    // everything by default.
    match /{document=**} {
      allow read, write: if false;
    }
  }
}
